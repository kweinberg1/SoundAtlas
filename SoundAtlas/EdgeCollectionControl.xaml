<UserControl x:Class="SoundAtlas.EdgeCollectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:soundatlas="clr-namespace:SoundAtlas"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ListBox ItemsSource="{Binding Path=Edges}">
        <ListBox.ItemTemplate>
            <DataTemplate DataType="{x:Type soundatlas:Edge}">
                <soundatlas:Edge DataContext="{Binding}"/>
            </DataTemplate>
        </ListBox.ItemTemplate>
        <!--<Line
            X1="0" Y1="15"
            X2="55" Y2="100"
            Stroke="Black"
            StrokeThickness="1" />
        <Line
            X1="0" Y1="15"
            X2="50" Y2="200"
            Stroke="Black"
            StrokeThickness="1" />-->
    </ListBox>
</UserControl>
